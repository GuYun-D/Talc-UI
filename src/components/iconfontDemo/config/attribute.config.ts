import type { IAttributeConfig } from "../../../base-ui/attribute/types/types";

export const attributeConfig: IAttributeConfig = {
  attributeTitle: "TIcon Attribute",
  attributeItems: [
    {
      name: "icon",
      desc: "图标名称",
      type: "String",
      value: "_",
      defaultValue: "required",
    }
  ],
};
